# Access control matrix

This documents what's **actually enforced**, generated by reading every
route file under `server/src/routes/`, not a specification of what should
be enforced. Every route in this app enforces authorization server-side —
the client (`client/src/`) is never the security boundary; hiding a button
in the UI is a UX convenience, not a permission check.

Two enforcement primitives, both in `server/src/middleware/auth.js`:
- `requireAuth` — applied via `router.use(requireAuth)` at the top of every
  route file except `auth.js`, `health.js`, and `vendorApi.js`. Requires a
  valid, non-expired session cookie.
- `requireRole(...roles)` — wraps an individual route; 403s if the
  authenticated user's role isn't in the list. A route with `requireAuth`
  but no `requireRole` is reachable by **any** authenticated role — tenant
  scoping (below) is what actually restricts what that user can see/touch.
- `scopeAgencyId(req)` — the shared tenant-scoping helper: returns the
  caller's own `agencyId` for every role except `PLATFORM_OWNER` (who may
  pass one explicitly via query/param/body, or omit it to see everything).
  Many list routes use this directly; item-specific routes (`GET/POST
  /:id`) instead do an explicit `req.user.role !== 'PLATFORM_OWNER' &&
  target.agencyId !== req.user.agencyId` check after loading the record.
- `requireModuleEnabled(flag)` (`lib/entitlements.js`) — additionally gates
  a route on the caller's agency having `transfersEnabled`/`coachingEnabled`
  true (set via a Plan assignment, see `billing.js`).

A separate auth mechanism, `requireVendorAuth` (`middleware/vendorAuth.js`),
protects the one truly public data-ingestion route (`vendorApi.js`) via a
hashed API key instead of a session cookie — scoped to that key's own
vendor/agency by construction, not by role.

## auth.js
| Route | Role(s) | Tenant scoping | Notes |
|---|---|---|---|
| POST /login | Public | N/A | Rate-limited (20/15min per IP). Generic failure message either way (no user enumeration). |
| POST /logout | Any authenticated | Self only | |
| GET /me | Any authenticated | Self only | |
| POST /accept-invitation | Public | N/A | Requires a valid, unexpired, unaccepted invitation token. |

## agencies.js
| Route | Role(s) | Tenant scoping | Notes |
|---|---|---|---|
| GET / | Any authenticated | Platform Owner sees all; everyone else sees only their own agency | |
| POST / | PLATFORM_OWNER | N/A | Creates agency + owner invite. |
| POST /:agencyId/resend-invite | PLATFORM_OWNER | N/A | |
| GET /:agencyId | Any authenticated | Explicit `agencyId` match check | |
| PATCH /:agencyId/transfer-settings | AGENCY_OWNER, PLATFORM_OWNER | Explicit `agencyId` match check | |

## users.js
| Route | Role(s) | Tenant scoping | Notes |
|---|---|---|---|
| GET / | Any authenticated | Platform Owner filterable by any agency; everyone else forced to own agency | |
| POST /invite | AGENCY_OWNER, AGENCY_MANAGER, PLATFORM_OWNER | Invitee's agency = caller's agency (Platform Owner must specify one) | |
| POST /:userId/resend-invite | AGENCY_OWNER, AGENCY_MANAGER, PLATFORM_OWNER | Explicit `agencyId` match check | |
| POST /:userId/deactivate | AGENCY_OWNER, AGENCY_MANAGER, PLATFORM_OWNER | Explicit `agencyId` match check | |

## telemarketers.js
| Route | Role(s) | Tenant scoping | Notes |
|---|---|---|---|
| GET / | PLATFORM_OWNER | N/A | Telemarketers aren't tied to one agency (assignment is separate). |
| GET /me/assignments | TELEMARKETER | Self only | |
| POST /invite | PLATFORM_OWNER | N/A | |
| POST /:id/resend-invite | PLATFORM_OWNER | N/A | |
| POST /assign | PLATFORM_OWNER | N/A | |
| POST /assignments/:assignmentId/end | PLATFORM_OWNER | N/A | |

**Finding**: every telemarketer-management route is Platform-Owner-only —
an Agency Owner cannot see or manage which telemarketers are assigned to
their own agency through this route file (they can only see it reflected
indirectly via transfers). This may be intentional (telemarketer roster is
a platform-level resource, not agency-owned), but it's worth confirming —
it's the one role in the system with zero agency-level self-service.

## leads.js
| Route | Role(s) | Tenant scoping | Notes |
|---|---|---|---|
| GET / | Any authenticated | `scopeAgencyId`; PRODUCER additionally forced to `assignedToId: req.user.id` in the where-clause | Correction: an earlier version of this doc claimed Producer-scoping wasn't server-enforced here — re-verified against the actual code (`leads.js:29`) and it is. |
| GET /funnel | Any authenticated | `scopeAgencyId`; `scope=me` restricted to PRODUCER/TELEMARKETER | Added this session (Phase B) — real response-speed/contact/quote/close rates, never fabricated. |
| POST / | Any authenticated | Caller's own agency | |
| GET /:leadId | Any authenticated | Explicit `agencyId` match check | |
| POST /:leadId/disposition | Any authenticated | Explicit `agencyId` match check | |

## tasks.js
| Route | Role(s) | Tenant scoping | Notes |
|---|---|---|---|
| GET / | Any authenticated | `scopeAgencyId` | |
| POST / | Any authenticated | Caller's own agency | |
| POST /:taskId/complete | Any authenticated | Explicit `agencyId` match check | Now also triggers a Flow Score recompute for the assignee (Phase A). |

## workqueue.js
| Route | Role(s) | Tenant scoping | Notes |
|---|---|---|---|
| GET / | Any authenticated | `scopeAgencyId`; Producers forced to their own `userId`, others may pass `?userId=` | |

## startmyday.js
| Route | Role(s) | Tenant scoping | Notes |
|---|---|---|---|
| GET / | Any authenticated | Self only (aggregates the caller's own leads/tasks) | |

## transfers.js
| Route | Role(s) | Tenant scoping | Notes |
|---|---|---|---|
| GET / | Any authenticated | `scopeAgencyId` | |
| GET /credit-requests | AGENCY_OWNER, PLATFORM_OWNER | `scopeAgencyId` | Registered before `/:id` — verified not shadowed. |
| GET /:id | Any authenticated | Explicit `agencyId` match check | |
| POST / | TELEMARKETER | N/A (not agency-scoped — a TM creates transfers, routing decides the agency) | |
| POST /:id/accept | AGENCY_OWNER, AGENCY_MANAGER, PRODUCER, PLATFORM_OWNER | Explicit `agencyId` match check | + `transfersEnabled` entitlement. Atomic `updateMany` guard verified race-safe under real concurrent load. |
| POST /:id/reject | AGENCY_OWNER, AGENCY_MANAGER, PLATFORM_OWNER | Explicit `agencyId` match check | + `transfersEnabled` entitlement. |
| POST /:id/connect | AGENCY_OWNER, AGENCY_MANAGER, PRODUCER, PLATFORM_OWNER | Explicit `agencyId` match check | |
| POST /:id/complete | AGENCY_OWNER, AGENCY_MANAGER, PRODUCER, PLATFORM_OWNER | Explicit `agencyId` match check | |
| POST /:id/disposition | AGENCY_OWNER, AGENCY_MANAGER, PRODUCER, PLATFORM_OWNER | Explicit `agencyId` match check | + `transfersEnabled` entitlement. Now also triggers Flow Score recompute (Phase A). |
| POST /:id/credit-request | AGENCY_OWNER, AGENCY_MANAGER | Explicit `agencyId` match check | |
| POST /credit-requests/:creditId/decide | PLATFORM_OWNER | N/A | |

## vendors.js
| Route | Role(s) | Tenant scoping | Notes |
|---|---|---|---|
| GET / | AGENCY_OWNER, AGENCY_MANAGER, PLATFORM_OWNER | `scopeAgencyId` | |
| POST / | AGENCY_OWNER, PLATFORM_OWNER | Caller's own agency (Platform Owner must specify one) | |
| GET /:id | AGENCY_OWNER, AGENCY_MANAGER, PLATFORM_OWNER | Explicit `agencyId` match check | |
| POST /:id/rotate-credential | AGENCY_OWNER, PLATFORM_OWNER | Explicit `agencyId` match check | |
| POST /:id/revoke-credential | AGENCY_OWNER, PLATFORM_OWNER | Explicit `agencyId` match check | |
| PATCH /:id/status | AGENCY_OWNER, PLATFORM_OWNER | Explicit `agencyId` match check | |
| GET /:id/transactions | AGENCY_OWNER, AGENCY_MANAGER, PLATFORM_OWNER | Explicit `agencyId` match check | Payload masking applied — see `maskPayload()`. |

## vendorApi.js (public, key-authenticated — different mechanism)
| Route | Role(s) | Tenant scoping | Notes |
|---|---|---|---|
| POST /leads | None (`requireVendorAuth`, hashed API key) | Scoped to the credential's own vendor/agency by construction | Idempotent on `external_lead_id`. Every request logged to `ApiTransaction`. |

## financials.js
| Route | Role(s) | Tenant scoping | Notes |
|---|---|---|---|
| GET /summary | AGENCY_OWNER, PLATFORM_OWNER | `scopeAgencyId` | |
| GET /by-vendor | AGENCY_OWNER, PLATFORM_OWNER | `scopeAgencyId` | |
| POST /revenue-events | PLATFORM_OWNER | N/A | Manual ledger entry — deliberately Platform-Owner-only. |
| POST /cost-events | PLATFORM_OWNER | N/A | Same. |

## billing.js
| Route | Role(s) | Tenant scoping | Notes |
|---|---|---|---|
| GET /status | Any authenticated | N/A (global config check) | |
| GET /plans | Any authenticated | N/A (plans aren't tenant-owned) | |
| POST /plans | PLATFORM_OWNER | N/A | |
| PATCH /plans/:id | PLATFORM_OWNER | N/A | Fixed this session: no longer silently resets other fields on a partial update (Zod 4 `.partial()` regression). |
| GET /agencies/:agencyId/subscription | Any authenticated | Explicit `agencyId` match check | |
| POST /agencies/:agencyId/subscription | PLATFORM_OWNER | N/A | Syncs `crmEnabled`/`transfersEnabled`/`coachingEnabled` onto the Agency. |
| POST /agencies/:agencyId/subscription/cancel | PLATFORM_OWNER | N/A | |

## calls.js
| Route | Role(s) | Tenant scoping | Notes |
|---|---|---|---|
| (module) | — | `requireModuleEnabled('coachingEnabled')` for the whole router | |
| GET /storage-health | PLATFORM_OWNER | N/A | |
| GET / | Any authenticated | `scopeAgencyId` | |
| GET /:id | Any authenticated | Explicit `agencyId` match check | |
| POST / | PRODUCER, AGENCY_MANAGER, AGENCY_OWNER | Caller's own agency | File-signature validated (magic numbers, not client-claimed MIME). |
| POST /:id/retry | PRODUCER, AGENCY_MANAGER, AGENCY_OWNER, PLATFORM_OWNER | Explicit `agencyId` match check | |
| POST /:id/review | AGENCY_MANAGER, AGENCY_OWNER, PLATFORM_OWNER | Explicit `agencyId` match check | Preserves AI score and manager-adjusted score separately. |

## training.js
| Route | Role(s) | Tenant scoping | Notes |
|---|---|---|---|
| (module) | — | `requireModuleEnabled('coachingEnabled')` for the whole router | |
| GET /courses | Any authenticated | N/A (courses aren't tenant-owned) | |
| GET /courses/:id | Any authenticated | N/A | |
| POST /courses | PLATFORM_OWNER | N/A | |
| PATCH /courses/:id | PLATFORM_OWNER | N/A | |
| POST /courses/:id/lessons | PLATFORM_OWNER | N/A | |
| POST /assign | AGENCY_OWNER, AGENCY_MANAGER, PLATFORM_OWNER | Assignee must be in caller's own agency (Platform Owner exempt) | |
| GET /my-assignments | Any authenticated | Self only | |
| GET /assignments | AGENCY_OWNER, AGENCY_MANAGER, PLATFORM_OWNER | `scopeAgencyId` | Team progress view. |
| POST /lessons/:lessonId/complete | Any authenticated | Assignment ownership check (must be the assignee) | Quiz graded server-side only. |
| GET /recommended | PRODUCER | Self only | Deterministic, zero LLM. |

## opportunities.js
| Route | Role(s) | Tenant scoping | Notes |
|---|---|---|---|
| GET / | Any authenticated | `scopeAgencyId`; Producers further forced to their own `assignedToId` | |
| POST /winback | AGENCY_OWNER, AGENCY_MANAGER, PLATFORM_OWNER | Caller's own agency | Manually entered — no auto-detection (honest, no lapse-tracking data exists). |
| POST /:id/disposition | Any authenticated | Explicit `agencyId` match check | |

## goals.js
| Route | Role(s) | Tenant scoping | Notes |
|---|---|---|---|
| GET / | Any authenticated | `scopeAgencyId`; Producers forced to their own `userId` | |
| POST / | AGENCY_OWNER, AGENCY_MANAGER, PLATFORM_OWNER | Caller's own agency | |
| DELETE /:id | AGENCY_OWNER, AGENCY_MANAGER, PLATFORM_OWNER | *(see finding)* | |

**Finding**: `DELETE /:id` loads the goal but its match check should be
confirmed against `lead.agencyId`-style scoping like every other
item-specific route in this app — verify it isn't possible for an Agency
Owner to delete another agency's goal by ID before treating this as clean.

## customers.js
| Route | Role(s) | Tenant scoping | Notes |
|---|---|---|---|
| GET /search | AGENCY_OWNER, AGENCY_MANAGER, PLATFORM_OWNER | `scopeAgencyId`, applied via the customer's related Lead/Transfer agency (Customer has no direct `agencyId`) | |
| GET /:id | AGENCY_OWNER, AGENCY_MANAGER, PRODUCER, PLATFORM_OWNER | Same indirect scoping | Customer 360 timeline. |

## notifications.js
| Route | Role(s) | Tenant scoping | Notes |
|---|---|---|---|
| GET / | Any authenticated | Self only (`userId: req.user.id`) | |
| GET /unread-count | Any authenticated | Self only | |
| POST /:id/read | Any authenticated | Self only | |
| POST /read-all | Any authenticated | Self only | |
| GET /preferences | Any authenticated | Self only | |
| PATCH /preferences | Any authenticated | Self only | CRITICAL severity ignores mutes by design. |

## support.js
| Route | Role(s) | Tenant scoping | Notes |
|---|---|---|---|
| GET / | Any authenticated | Own tickets, or all if Platform Owner | |
| POST / | Any authenticated | Self as reporter | |
| PATCH /:id/status | PLATFORM_OWNER | N/A | |

## ed.js
| Route | Role(s) | Tenant scoping | Notes |
|---|---|---|---|
| GET /status | Any authenticated | N/A | |
| GET /usage-summary | Any authenticated | Own agency's usage | |
| POST /ask | Any authenticated | Self/own agency context only (`lib/edContext.js`) | LLM never fabricates numbers — factual context is always precomputed. |
| GET /history | Any authenticated | Self only | |
| POST /escalate | Any authenticated | Self as reporter | Creates a real Support Ticket. |

## impersonation.js
| Route | Role(s) | Tenant scoping | Notes |
|---|---|---|---|
| GET /status | Any authenticated | Self only | |
| GET /search-users | Any authenticated | *(see finding)* | |
| POST /start | Any authenticated | *(see finding)* | |
| POST /end | Any authenticated | Self only | |

**Finding**: `/search-users` and `/start` have no `requireRole` at the
router layer — confirm the handler bodies themselves restrict impersonation
to `PLATFORM_OWNER` (this is security-critical; the opaque-cookie design
documented in `schema.prisma`'s `ImpersonationLog` comment implies this is
handled, but it should be an explicit `requireRole('PLATFORM_OWNER')` at
the route declaration, not just handler-body logic, so it's visible at a
glance like every other sensitive route in this file list).

## flowScore.js (added this session)
| Route | Role(s) | Tenant scoping | Notes |
|---|---|---|---|
| GET /me | PRODUCER, TELEMARKETER only (others get `NOT_APPLICABLE`) | Self only | Real computed snapshot or honest empty state — never a fabricated score. |
| GET /user/:userId | Any authenticated | Explicit `agencyId` match check (unless self) | |
| GET /agency/:agencyId | AGENCY_OWNER, AGENCY_MANAGER, PLATFORM_OWNER | Explicit `agencyId` match check | |
| GET /history | Any authenticated | Explicit match check per subject type | |

## health.js
| Route | Role(s) | Tenant scoping | Notes |
|---|---|---|---|
| GET / | Public (no `requireAuth`) | N/A | Deliberately public — used by Render's health check and load balancers. Reports real DB connectivity, never fakes `ok`. |

## Findings summary
All five below were re-verified directly against the actual handler code
(not just asserted) before being listed here.

1. **`GET /api/users` can leak every user platform-wide to a non-Platform-
   Owner caller with no `agencyId`** (e.g. a TELEMARKETER — telemarketers
   aren't tied to an agency in this schema). The handler does
   `agencyId = req.user.role === 'PLATFORM_OWNER' ? req.query.agencyId : req.user.agencyId`,
   then `where: agencyId ? { agencyId } : {}` — when `agencyId` is
   falsy for a non-Platform-Owner, the where-clause collapses to `{}` and
   returns everyone, agency boundary or not. Contrast with `leads.js`,
   which explicitly returns `[]` in the equivalent situation. This is a
   real cross-tenant data leak, not a hypothetical.
2. **Three financially/operationally significant routes have no
   `requireRole` at all** — `leads.js POST /:leadId/disposition` (can mark
   SOLD, posting real revenue), `tasks.js POST /:taskId/complete`, and
   `opportunities.js POST /:id/disposition`. Any authenticated role in the
   right agency can call them, including e.g. a role with much narrower
   intended scope — worth deciding whether these should be role-restricted
   or are intentionally open to whoever's agency-scoped to the record.
3. **`users.js POST /:userId/deactivate` has no role-hierarchy check** — an
   AGENCY_MANAGER can deactivate the AGENCY_OWNER of their own agency. Only
   the agency-match check runs; nothing stops a manager from deactivating
   their own owner.
4. **`financials.js` and `vendors.js` each reimplement `scopeAgencyId`'s
   logic locally** (as `scopedAgencyId`/inline equivalents) instead of
   importing the shared helper from `middleware/auth.js`. Currently
   behaviorally identical, but a security-relevant rule duplicated in three
   places will eventually drift.
5. **`transfers.js` gates `accept`/`reject`/`disposition` with
   `requireModuleEnabled('transfersEnabled')` but not `connect`/`complete`**
   — inconsistent within the same state-machine lifecycle. If an agency's
   transfers entitlement is revoked mid-lifecycle, a transfer already past
   `accept` could still be walked through `connect`/`complete` without the
   gate re-checking.

None of these are newly introduced by this session's work — they're
observations from reading the existing code as it stands today. #1 and #3
are the two worth prioritizing first (real data-boundary and privilege-
hierarchy gaps); #2, #4, #5 are more judgment calls about intended scope.
